# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native builds (this project stays managed; these are generated by prebuild)
/ios
/android
*.orig.*

# Metro
.metro-health-check*

# Testing
coverage/

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# Local env — ThinkTwice V1 has no backend and needs no secrets,
# but never commit anything that shows up here.
.env
.env.*
!.env.example

# Editors
.idea/
.vscode/*
!.vscode/extensions.json
*.tsbuildinfo
